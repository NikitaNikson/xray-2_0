#include "common.h"

uniform sampler s_base;

//////////////////////////////////////////////////////////////////////////////////////////
// Pixel
float4 main ( p_TL I ) : SV_Target
{
	return t_base.Sample( smp_linear, I.Tex0 )*I.Color;
//	return float4(1,1,1,1);

//	float4 res = s_base.Sample( smp_base, I.Tex0 )*I.Color;
//	clip(res-m_AlphaRef);
//	clip(res-0.5);
//	return res;
}
